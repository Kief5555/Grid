<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= file.title %>
    </title>
    <meta name="description" content="View this <@= file.ext&> on PrintedWaste grid. ID: <%= file.id %>">
    <meta name="theme-color" content="#00aa00">
    <meta property="og:title" content="<%= file.title %>">
    <meta property="og:description" content="View this <@= file.ext&> on PrintedWaste grid. ID: <%= file.id %>">
    <meta property="og:url" content="<%= file.redirectUrl %>">
    <% if (file.image) { %>
        <meta property="og:image" content="<%= file.image %>">
        <% } %>
            <style>
                body {
                    background-color: #7289DA;
                    /* Discord blurple color */
                }
            </style>
            <script>
                setTimeout(() => {
                    window.location.replace("<%= file.redirectUrl %>");
                }, 100);
            </script>
</head>

<body>
    <!-- This content will not be shown as there's an instant redirect -->
</body>

</html>